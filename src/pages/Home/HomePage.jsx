/** Placeholder — Home (dicionário) ainda em `legacy/pages/home.js` + `index.html`. */
export default function HomePage() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>Home (React)</h1>
      <p>Esta rota será a migração do dicionário de ruas. Por enquanto use <a href="./index.html">index.html</a>.</p>
    </main>
  )
}
